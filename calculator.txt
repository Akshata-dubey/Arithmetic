

        // Call the addNumbers method to add the numbers
        int result = addNumbers(num1, num2);

        // Print the result
        System.out.println("The sum is: " + result);
    }

    // Method to add two numbers
    public static int addNumbers(int a, int b) {
        return a + b;
    }
}

